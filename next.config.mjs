/** @type {import('next').NextConfig} */
const nextConfig = {

    images: {
        domains: [
            'images.source.unsplash.com',
            'source.unsplash.com',
        ]
    },
};

export default nextConfig;
